openapi: 3.1.0
info:
  title: raphael-messaging
  version: 0.1.0
  description: DMs, group channels, workspace channels
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
